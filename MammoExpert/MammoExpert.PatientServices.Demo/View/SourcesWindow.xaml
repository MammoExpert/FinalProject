<Window
    x:Class="MammoExpert.PatientServices.Demo.View.SourcesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:local="clr-namespace:MammoExpert.PatientServices.Demo.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:MammoExpert.PatientServices.Demo.ViewModel"
    Title="Источники данных"
    Width="500"
    Height="300"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ToolWindow"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <vm:SourcesWindowViewModel x:Key="ViewModelSource" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid DataContext="{Binding Source={StaticResource ViewModelSource}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Margin="2"
            Text="Тип источника данных:" />

        <ComboBox
            x:Name="cbSourceType"
            Grid.Row="1"
            Grid.Column="0"
            Margin="2"
            ItemsSource="{Binding SourceTypes}"
            SelectedIndex="0"
            SelectedItem="{Binding SelectedType}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SourceTypeChanging}" CommandParameter="{Binding ElementName=cbSourceType, Path=SelectedValue}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ComboBox>

        <ListView
            Grid.Row="2"
            Grid.RowSpan="3"
            Grid.Column="0"
            Margin="2"
            ItemsSource="{Binding Path=Sources}"
            SelectedItem="{Binding SelectedSource}">

            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="#" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Type}" Header="Тип" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Description}" Header="Описание" />
                </GridView>
            </ListView.View>
        </ListView>

        <Button
            Grid.Row="1"
            Grid.Column="1"
            Width="100"
            Margin="2"
            Command="{Binding AddSource}"
            CommandParameter="{Binding SelectedSource}"
            Content="Добавить..." />
        <Button
            Grid.Row="2"
            Grid.Column="1"
            Width="100"
            Margin="2"
            Command="{Binding EditSource}"
            Content="Редактировать..." />
        <Button
            Grid.Row="3"
            Grid.Column="1"
            Width="100"
            Margin="2"
            Command="{Binding DeleteSource}"
            Content="Удалить" />

    </Grid>
</Window>
